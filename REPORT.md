# 나만의 뉴스레터 에이전트 — 증시 뉴스 다이제스트

## 1. 분야 및 독자 정의

**분야**: 증시(투자) 뉴스 — 실제로 매일 F&G(공포·탐욕) 지수 기반 역발상 투자를 하고
있는 개인 투자자 관점에서, 국내외 증시에 영향을 주는 소식을 매일 아침 자동으로
받아보는 시스템을 만들었다.

**독자 페르소나**:
- 누구: F&G 지수 기반으로 역발상 투자를 하는 개인 투자자 — 나스닥/TQQQ 비중이 큼
- 이미 아는 것: 주식 기본 용어, 코스피·나스닥·S&P500 지수 개념, 금리·물가가 증시에 미치는 영향
- 원하는 것: "오늘 매매 판단에 영향을 줄 만한 소식"만 골라서, 숫자(등락률·수치)가
  정확하게 들어간 요약

(전체 정의는 `audience.yaml` 참고)

## 2. 소스 채택표

소스 6곳을 실제로 재보고(`_measure_sources.py`로 측정 — 이 파일은 채택 과정만
남기고 최종 제출에서는 뺐다) 4곳을 채택, 1곳을 탈락시켰다.

| 소스 | 24h 이내 건수 | G1(본문 600자 이상) | 채택 여부 | 근거 |
|---|---|---|---|---|
| 한국경제 (feed/finance) | 50건 | 통과(1,378자) | ✅ 채택 | 24시간 내내 꾸준히 발행, 국내 시장 커버 |
| 연합뉴스 경제 | 120건 | 통과(1,069자) | ✅ 채택 | 통신사라 사실 위주, 압도적 발행량 |
| Investing.com (news_25) | 10건 | 통과(3,264자) | ✅ 채택 | 건수는 적지만 10건 전부 24h 이내(생존 양호), 해외 시황 보강 |
| Yahoo Finance | 50건 중 11건만 24h 이내 | 통과(5,400자) | ✅ 채택 | 재고보다는 느리지만 최소 기준은 충족, 해외 소스 다양화 |
| CNBC Markets | 30건 중 **1건만** 24h 이내 | 통과(7,264자) | ❌ 탈락 | **G2(생존) 미달** — 본문 품질은 좋지만 이 RSS 섹션이 하루 1건꼴로만 갱신돼 매일 도는 파이프라인에 못 씀 |

(측정 스크립트가 UTF-8 인코딩 문제로 콘솔에 깨져 보이는 걸 발견 → 파일 출력 시
`PYTHONIOENCODING=utf-8`을 강제해야 한다는 것도 확인함.)

## 3. 선별 로직 설계

**중요도 판단 문장** (`audience.yaml`의 `중요도_기준`, 예/아니오로 답할 수 있게 작성):
- 미국 연준(Fed)의 금리 결정이나 통화정책 방향이 새로 나왔거나 바뀌었나
- 코스피·나스닥·S&P500 등 주요 지수가 하루 만에 1% 이상 크게 움직였나
- 국제 유가·환율·국채금리처럼 시장 전반에 영향을 주는 거시 변수가 크게 변했나
- 지금 쓰는 매매 전략(F&G 기반 역발상 매매)의 판단 기준에 영향을 줄 만한 소식인가

**예선/본선 2단계 구조**: 하루 수집량이 150~200건(연합뉴스 혼자 120건대)이라 한
화면에 다 놓고 비교할 수 없다. 그래서:
- 예선: 30건씩 묶어(하루 수집량 ÷ 30 ≈ 5~7묶음) 묶음마다 5건씩 통과시킨다.
  30건이라는 크기는 실측 수집량(191건/7묶음≈27건)에 맞춰 정한 값이다.
- 본선: 예선 통과분(25~35건) 전체를 한 화면에 놓고 상대평가로 최종 5건을 고른다.
  이때 각 선택에 `event`(사건 라벨)를 붙이게 해서 같은 사건 중복을 막게 지시한다.

**선별 기준이 실제로 동작했다는 근거(로그)**:
```
② 선별   191건 → 묶음 7개 → 예선 35건 → 본선 5건 → 중복/상한 제거 1건 → 최종 4건
   매체상한 제외: 금리·유가 상승에 'AI 속도조절론'까지…SK하닉 AD
```
프롬프트로 "같은 사건은 하나만"이라고 부탁했지만(어제 실습에서 확인했듯 이건
확률적으로만 지켜진다), **코드로 한 번 더 걸러야** 확실하다. 그래서 본선 결과에
event 라벨 중복 제거 + 매체(source)당 최대 2건 상한을 코드로 강제했고(`graph.py`의
`_dedup_and_cap`), 실제로 위 로그처럼 한 건이 매체 상한에 걸려 제외되는 것을 확인했다.

## 4. 파이프라인 구조도

```mermaid
graph TD;
	__start__([시작]):::first
	collect(① 수집)
	select(② 선별<br/>예선→본선)
	report_one_node(③ 요약+인사이트<br/>기사마다 병렬)
	verify(④ 검수<br/>실패시 재생성/스킵)
	publish(⑤ 발행<br/>디스코드+지표기록)
	__end__([끝]):::last
	__start__ --> collect;
	collect --> select;
	select -.팬아웃.-> report_one_node;
	select -.0건이면 바로.-> verify;
	report_one_node --> verify;
	verify --> publish;
	publish --> __end__;
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
```

State(`NewsletterState`): `hours`(설정) · `collected`/`picked`(단계 결과, 덮어쓰기) ·
`drafted`(팬아웃 워커들이 각자 채움, `operator.add` 리듀서) · `verified`(검수 통과분) ·
`log`(모든 노드가 한 줄씩 남김, `operator.add` 리듀서).

## 5. 실행 기록

**1차 실행(6시간 창, dry-run)** — 파이프라인이 실제로 예외를 만나고 처리하는 것까지 확인:
```
① 수집   6시간 창 · 141건 (소스 4곳, 무응답/0건: ['Yahoo Finance'])
② 선별   141건 → 묶음 5개 → 예선 25건 → 본선 5건 → 중복/상한 제거 1건 → 최종 4건
   매체상한 제외: 코스피 급락에도 은행주는 상승한 네 가지 이유
   취재 실패(파싱 오류 JSON을 찾을 수 없음: ...): 美 3년 만에 금리 인상 나서나…
   취재 완료: 美 국채 5% 돌파, 뉴욕증시 급락
   취재 완료: 8월 수출물가 3.7% 급락
   취재 완료: 사우디 송유관 피격, 유가 급등
④ 검수 불합격(본문에 없는 숫자: {'500,'}), 재생성 시도: 금리·유가 상승에 'AI 속도조절론'까지…
   재생성 후 합격: 국채금리 5% 돌파, 뉴욕증시 급락
④ 검수 합격: 8월 수출물가 3.7% 급락
④ 검수 합격: 사우디 송유관 피격, 유가 급등
⑤ 발행   3건 · dry-run (실제 발송 안 함)
```

**2차 실행(24시간 창, 실제 발행)**:
```
① 수집   24시간 창 · 191건 (소스 4곳, 무응답/0건: 없음)
② 선별   191건 → 묶음 7개 → 예선 35건 → 본선 5건 → 중복/상한 제거 1건 → 최종 4건
   매체상한 제외: 금리·유가 상승에 'AI 속도조절론'까지…SK하닉 AD
   취재 완료: 美 9월 금리인상 가능성 92.5%로 급등
   취재 실패(본문 부족): [속보] 삼전닉스 또 출렁…코스피, 3.3% 급락해 6
   취재 실패(파싱 오류 Expecting ',' delimiter: line 1 column 219): 사우디 송유관 폐쇄에 국제유가 상승…
   취재 완료: 8월 수출물가 3.7%↓, 3년8개월來 최대낙폭
④ 검수 합격: 美 9월 금리인상 가능성 92.5%로 급등
④ 검수 합격: 8월 수출물가 3.7%↓, 3년8개월來 최대낙폭
⑤ 발행   2건 · 성공
```
디스코드 채널("뉴스레터-에이전트")에 위 2건이 실제로 발행됐고, `store/metrics.jsonl`에
두 실행 모두 기록이 남았다(`published: true`인 두 번째 줄이 실제 발행분).

## 6. 프로젝트 회고

**가장 공들인 부분**: 검수(④) 단계다. 처음엔 "숫자가 본문에 있는지"만 문자열로
대조했는데, 실제로 돌려보니 "5%"라는 표현이 원문엔 "5%를 넘어"로, 요약엔 "5%포인트"
처럼 다르게 적히거나, 쉼표(`500,`) 하나가 숫자로 오인식되는 등 오탐이 나왔다. 그래도
"실패하면 그냥 버리는" 대신 **한 번 재생성**하게 만들어서, 실제로 1차 실행에서
재생성 후 합격하는 케이스를 직접 확인했다 — 검수 실패가 곧바로 발행 중단으로
이어지지 않는다는 걸 실제 로그로 증명한 셈이다.

**향후 개선하고 싶은 점**:
1. 검수를 문자열 대조 대신 "숫자만 따로 뽑아 원문과 다시 물어보는" 방식으로
   바꾸면 쉼표·단위 표기 차이로 인한 오탐을 줄일 수 있을 것 같다.
2. 어제 프롬프트 체이닝 실습 때 "선별" 개선점으로 정리했던 이벤트 중복·매체 상한
   문제를, 이번엔 처음부터 코드로 넣었다 — 실제로 2차 실행에서 매체 상한이
   작동하는 걸 확인해서, "프롬프트 부탁은 확률적으로만 지켜지니 코드로 한 번 더
   확인해야 한다"는 원칙이 이번에도 그대로 맞아떨어졌다.
3. Yahoo Finance처럼 24시간 내 갱신이 적은 소스는 시간 창을 넓히거나(예: 48시간)
   별도 취급이 필요해 보인다.
