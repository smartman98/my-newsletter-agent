# AI Agent 프로젝트 Peer Review Template (PRT)

- 코더: My AI Work Radar 작성자 (이름 미상 — 공유해주신 리포트 기준으로 작성)
- 리뷰어: 조아라

(참고: 리포지토리 링크를 직접 받지 못해서, 공유해주신 리포트 본문 내용만으로 리뷰했어요.
코드를 직접 clone해서 돌려보지는 못했다는 점을 먼저 밝혀요.)

---

## PRT (Peer Review Template)

### 1. 실행 가능성 (Executability)

에이전트가 실제로 돌아가는가를 확인하는 항목입니다.

- [x] 코더가 남긴 실행 결과(로그·캡처·데모)를 확인했다
- [ ] (선택) 직접 clone 해서 실행해봤다 → 리포지토리 링크가 없어서 못 해봤음
- 막혔다면 어디서: 코드 자체를 못 본 게 한계. 다만 5.1 "실행 기록" 표(Collected 78 →
  Normalized 78 → Prefiltered 34 → Ranked 34 → Selected 5 → Validated 5 → Skipped 0 →
  Retry Count 0 → Publish Result SUCCESS)가 각 단계별 개수를 전부 숫자로 남겨놨고,
  5.2에 Gmail로 실제 수신된 화면 캡처(개인정보는 가림)까지 있어서, 텍스트 로그만 있는
  것보다는 훨씬 믿음이 갔다.
- 한 줄 코멘트: 실행 증거가 "숫자 표 + 실제 수신 캡처" 두 겹으로 있어서 설득력은 있지만,
  내가 직접 눌러보거나 clone해서 재현해본 게 아니라서 "정말 매번 이렇게 되는가"는
  확인 못 했다. GitHub 링크나, (조아라 프로젝트처럼) 로그인 없이 실행해볼 수 있는
  링크가 있었으면 더 확실히 검증할 수 있었을 것 같다.

### 2. 아키텍처 가독성 (Architecture Legibility)

에이전트의 제어 흐름이 코드에서 얼마나 잘 드러나는가를 보는 항목입니다.

- [x] 그래프 그림이나 구조 설명이 있다 (단, 실제 이미지는 못 보고 설명 텍스트만 봄)
- [x] 노드 이름만 봐도 무슨 일을 하는지 짐작된다
- [x] 왜 이 패턴을 골랐는지 한 줄이라도 적혀 있다
- 코멘트: collect → normalize → prefilter → rank → select → (본문수집) → summarize →
  verify → publish로 이어지는 흐름이 이름만 봐도 순서가 그려진다. 4.3에 "검수 결과에
  따라 재작성·재검수·제외라는 상태 변화까지 관리해야 해서 StateGraph를 골랐다"는
  선택 이유도 명확히 적혀 있고, 특히 "특정 기사 검수 실패를 전체 파이프라인 실패와
  분리하는 데 적합하다"는 문장이 이 프로젝트가 실제로 겪은 문제(기사 하나가 실패해도
  전체가 멈추면 안 된다)와 바로 연결돼서 왜 이 구조가 필요했는지 납득이 됐다.
- 가장 궁금했던 노드 / 부분: 실행 기록 표를 보면 Collected 78건과 Normalized 78건이
  완전히 같다. collect와 normalize를 굳이 별도 노드로 나눈 이유가 궁금하다 — 정규화
  단계에서 중복 제거나 형식 변환처럼 개수가 줄어들 수 있는 로직이 들어있는데 이번
  실행에서는 우연히 걸러진 게 없었던 건지, 아니면 normalize가 원래 개수를 줄이는
  역할이 아니라 필드 형식만 맞추는 역할이라 항상 같은 개수가 나오는 게 정상인 건지
  알고 싶다.

### 3. 측정 가능성 (Measurability)

성능을 숫자로 말할 수 있는가를 보는 항목입니다.

- [x] 성능을 나타내는 숫자가 하나라도 있다
- [x] 그 숫자를 어떻게 쟀는지(평가셋, 기준) 적혀 있다
- [x] 개선 전 → 후 비교가 있다 (단, 일부는 "비교 안 함"을 정직하게 밝힘)
- 코멘트: 소스 채택표(2.2)가 특히 좋았다. Google Cloud Blog 95.0%부터 LangChain
  Blog·Microsoft AI Blog 0.0%까지, 후보 7개를 전부 같은 방식(비교 기사 수 대비 관심
  기사 수 적중률)으로 재보 놓고 채택/탈락을 정했다. 더 인상 깊었던 부분은 3.2절에서
  예선 로직을 정규식 단어 경계 방식으로 고친 뒤, "이 변경에 대해 별도의 전·후 전체
  기사 수는 측정하지 않았으므로 근거 없는 수치 비교는 제시하지 않는다"고 직접 적어놓은
  것이다. 안 잰 걸 잰 것처럼 꾸미지 않고, 대신 "WARDOGS 안의 rag가 RAG로 오탐된
  사례가 재현됐고 그게 제거됐다"는 구체적 사례로 근거를 댄 점이 정직하고 신뢰가 갔다.
- 가장 궁금했던 부분: 8.2 향후 개선 항목에 "동일 출처 기사 편중을 막는 Source
  Diversity 제약"이 있는 걸 보면, 지금 TOP 5에 특정 소스(예: Google Cloud Blog,
  적중률 95%로 압도적 1위)가 쏠렸을 가능성이 있어 보인다. 이번 실행에서 실제로
  최종 5건이 소스별로 몇 개씩 나왔는지 궁금하다 — 만약 한 소스에 3~4개가 몰렸다면
  "적중률이 높다"는 게 "다양성이 없다"는 것과 같은 말이 될 수도 있어서다.

### 4. 견고성 (Robustness)

실패하는 경로가 설계되어 있는가를 보는 항목입니다.

- [x] 에러·예외 처리가 눈에 띈다 (도구 호출 실패, 빈 결과 등)
- [x] 폭주 방지 장치가 있다 (recursion_limit, 최대 턴 수, 타임아웃, 재시도 제한)
- [x] (보너스) 이상한 입력을 하나 넣어봤다 → 반응 확인
- 코멘트: 검수 실패 시 재작성은 기사당 최대 1회로 명확히 상한을 뒀고, 재작성 자체가
  또 실패하거나 재검수도 실패하면 그 즉시 SKIP 처리하고 다음 기사로 넘어가는 구조가
  적혀 있다(retry_count=1, final_result="SKIP"). 본문 수집 HTTP 요청에 timeout=15를
  건 것도 눈에 띈다. 가장 좋았던 건 6절 마지막 문장 — "현재 코드에는 별도의
  recursion_limit 설정은 없으며, 이 리포트는 실제 구현에 존재하는 제한만 기록한다"고
  스스로 한계를 밝힌 부분이다. 없는 안전장치를 있다고 쓰지 않은 정직함이 좋았다.
  이상한 입력 테스트로는 8.1에 "잘못된 날짜를 삽입한 Fault Injection에서 FAIL /
  number_error 탐지"가 언급돼 있어서, 검수 로직이 실제로 이상값을 걸러내는지 의도적
  으로 테스트해본 것도 확인됐다.
- 질문 1개 (코더에게): recursion_limit이 없다고 스스로 밝힌 만큼, 만약 재작성 후
  재검수도 계속 FAIL만 나는 상황이 반복된다면(예: 같은 기사가 재작성돼도 매번 같은
  숫자 오류를 만든다면) 이론적으로 무한 루프가 될 위험은 없는지, 아니면 애초에
  "최대 1회"라는 상한이 코드 레벨에서 재작성 함수 자체에 박혀 있어서 구조적으로
  불가능한 것인지 궁금하다.

### 5. 작업 추적성 (Traceability)

AI와 함께 만든 과정이 남아 있는가를 보는 항목입니다.

- [x] 작업 흔적이 남아 있다 (커밋 메시지, 프롬프트 기록, CLAUDE.md 등)
- [x] 코더가 직접 고친 부분이 무엇인지 알 수 있다
- 질문 1개 (코더에게): 커밋 이력에 적힌 네 개(cfa62fe 초기 프로젝트 추가, 64ca7b0
  리포트 구조도와 실행 증거 추가, cbb308f 뉴스레터 필드 검수 수정, 026179a 상태도
  표기 개선)를 보면 뒤 세 개 중 코드 로직 자체가 바뀐 건 cbb308f 하나이고 나머지
  둘은 리포트/구조도 같은 문서 보강으로 보인다. 실제로 프로젝트를 만드는 동안
  겪었던 시행착오(예: 3.2절의 정규식 오탐을 고친 순간, 검수 재작성 로직을 처음
  설계할 때 실패했던 시도 등)가 커밋 단위로도 남아있는지, 아니면 최종 형태로
  합쳐져서 한 번에 커밋된 건지 궁금하다 — 과정 자체를 커밋으로 추적하고 싶어서다.

---

## 리뷰어 총평 (3줄)

```
좋았던 점 1가지: 못 잰 걸 잰 것처럼 꾸미지 않은 점. 정규식 오탐 수정 효과를
전후 수치로 과장하지 않고 "측정 안 했다"고 직접 밝힌 것, recursion_limit이
없다는 한계도 숨기지 않고 리포트에 그대로 적은 점이 특히 신뢰가 갔다.

딱 하나만 바꾼다면: 최종 선정된 5건의 소스별 분포(예: Google Cloud 몇 건,
AWS 몇 건...)를 실행 기록 표에 같이 남기면 좋겠다. 향후 개선 항목으로 적어둔
"Source Diversity" 문제가 실제로 이번 실행에서 얼마나 심했는지를 바로 보여줄
수 있는 숫자라서다.

내 프로젝트에 가져가고 싶은 것: 검수 실패 → 재작성 1회 → 그래도 실패하면
SKIP하고 다음 기사로 넘어가는 상한이 명확한 재시도 구조. 내 프로젝트도 검수
불합격 시 1회 재생성하고 스킵하는 방식인데, "왜 1회인지"를 이 리포트처럼
recursion_limit 유무까지 같이 밝혀서 설명하는 방식을 참고하고 싶다.
```

코더가 질문에 코멘트로 답하면 리뷰 완료입니다.
