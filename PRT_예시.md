# AI Agent 프로젝트 Peer Review Template (PRT)

- 코더: 조아라
- 리뷰어: (상대방 이름)

---

## PRT (Peer Review Template)

### 1. 실행 가능성 (Executability)

에이전트가 실제로 돌아가는가를 확인하는 항목입니다.

- [x] 코더가 남긴 실행 결과(로그·캡처·데모)를 확인했다
- [x] (선택) 직접 clone 해서 실행해봤다 → 성공
- 막혔다면 어디서: 막힌 곳 없음. GitHub 저장소의 REPORT.md 5번 "실행 기록"에
  실제로 세 번 돌린 로그가 전부 그대로 남아있고, 그중 두 번은 디스코드에 실제
  발행까지 성공한 캡처 이미지도 함께 있어서 클론하지 않고도 실행 여부를
  바로 확인할 수 있었다. 추가로 로그인 없이 바로 실행해볼 수 있는 HTTPS
  주소도 만들어 놨어서(server.py, `/run?token=...`), 실제로 그 링크를 눌러
  6시간 창으로 한 번 더 돌려봤고 정상적으로 4건 선별, 2건 검수 통과까지
  확인했다.
- 한 줄 코멘트: 로그와 캡처, 거기에 직접 눌러볼 수 있는 링크까지 있어서
  "진짜 돌아가는지"를 의심할 이유가 전혀 없었다.

### 2. 아키텍처 가독성 (Architecture Legibility)

에이전트의 제어 흐름이 코드에서 얼마나 잘 드러나는가를 보는 항목입니다.

- [x] 그래프 그림이나 구조 설명이 있다
- [x] 노드 이름만 봐도 무슨 일을 하는지 짐작된다
- [x] 왜 이 패턴을 골랐는지 한 줄이라도 적혀 있다
- 코멘트: REPORT.md 4번에 Mermaid로 그린 전체 그래프가 있고, collect →
  select → report_one_node → verify → publish 순서로 노드 이름 자체가
  하는 일을 그대로 말해준다. 특히 select 노드 설명에 "하루 수집량이 190건대라
  한 화면에 다 놓고 비교할 수 없어서 30건씩 묶어 예선을 치르고 통과분을
  본선에서 상대평가한다"처럼, 왜 그 구조를 골랐는지 근거까지 적혀 있어서
  이해가 빨랐다.
- 가장 궁금했던 노드 / 부분: select 노드가 고른 기사가 0건일 때는
  report_one_node로 안 가고 곧바로 verify로 넘어가도록 조건부 라우팅
  (`fanout_to_report` 함수)이 되어 있던데, 이 분기를 안 넣었으면 실제로
  어떤 에러가 났을지, 그리고 이런 "빈 팬아웃" 케이스를 처음부터 염두에
  두고 설계한 건지 나중에 겪고 나서 추가한 건지 궁금했다.

### 3. 측정 가능성 (Measurability)

성능을 숫자로 말할 수 있는가를 보는 항목입니다.

- [x] 성능을 나타내는 숫자가 하나라도 있다
- [x] 그 숫자를 어떻게 쟀는지(평가셋, 기준) 적혀 있다
- [x] 개선 전 → 후 비교가 있다
- 코멘트: 소스 채택 과정에서 "24시간 이내 건수", "본문 추출 성공 시 글자
  수(600자 기준)", "HTTP 상태 코드(200/403/404)" 같은 구체적인 수치를
  실제로 RSS를 호출해서 측정한 뒤에 표로 정리해 뒀다. 더 인상적이었던 건
  CNBC Markets를 처음엔 "24시간 내 신규글 1건뿐"이라는 이유로 뺐다가, 그
  컷오프가 근거 없는 임의의 숫자였다는 지적을 받고 다시 넣어서 실제로
  돌려본 다음(수집량 191건 → 194건, 최종 선택 0건 기여) "빼도 되고 넣어도
  된다"는 걸 숫자로 재확인한 부분이다. 그냥 "성능 좋음"이 아니라 자기
  판단 자체를 실험으로 검증하는 태도가 보였다.

### 4. 견고성 (Robustness)

실패하는 경로가 설계되어 있는가를 보는 항목입니다.

- [x] 에러·예외 처리가 눈에 띈다 (도구 호출 실패, 빈 결과 등)
- [x] 폭주 방지 장치가 있다 (recursion_limit, 최대 턴 수, 타임아웃, 재시도 제한)
- [x] (보너스) 이상한 입력을 하나 넣어봤다 → 반응 확인
- 코멘트: report_one 노드는 본문이 600자 미만이면 그 기사를 조용히
  스킵하고 로그만 남기고, JSON 파싱에 실패하면 예외를 던지는 대신 빈
  결과를 돌려주도록 처리되어 있다. verify 노드는 검수에 불합격하면 딱
  1번만 재생성을 시도하고, 그래도 실패하면 그 기사를 최종 발행에서
  제외한다 — 무한 재시도로 빠지지 않게 상한이 명확했다. 가장 눈에 띈
  건 ask_claude 함수 자체에 try/except가 있다는 점이었다. 커밋 로그를
  보니 이게 원래는 없었는데, 실제로 Claude 호출이 "최대 턴 수 초과"로
  실패해서 파이프라인 전체가 죽은 사고를 겪은 뒤에 추가한 것이었다.
  이상한 입력 테스트로는, 콘텐츠가 거의 없는 소스(CNBC)를 일부러
  넣어봤는데 별도의 방어 코드 없이도 선별 경쟁 과정에서 자연스럽게
  탈락하는 걸 확인했다고 REPORT.md에 적혀 있었다.

### 5. 작업 추적성 (Traceability)

AI와 함께 만든 과정이 남아 있는가를 보는 항목입니다.

- [x] 작업 흔적이 남아 있다 (커밋 메시지, 프롬프트 기록, CLAUDE.md 등)
- [x] 코더가 직접 고친 부분이 무엇인지 알 수 있다
- 질문 1개 (코더에게): 예선(prelim) 단계에서 AI 응답의 JSON 파싱이
  실패하면 "그 묶음의 앞에서부터 그냥 채운다"는 안전 대체 로직으로
  넘어가던데, 이 경우 원래 중요도 순으로 고른 게 아니라 그냥 목록 순서로
  대체되는 셈이라 실제로 안 좋은 기사가 통과할 위험이 있어 보인다.
  이 트레이드오프(파이프라인을 안 죽게 하는 것 vs 대체값의 품질)를 어떻게
  판단해서 이렇게 설계했는지 궁금하다.

---

## 리뷰어 총평 (3줄)

```
좋았던 점 1가지: 소스 채택 기준을 감이 아니라 실측 수치로 검증하고, 처음
판단이 틀렸다는 것도 숨기지 않고 정정 과정 자체를 REPORT.md에 남긴 점.

딱 하나만 바꾼다면: 검수(④)의 숫자 대조 방식이 쉼표나 단위 표기 차이로
오탐이 나는 경우가 있었다고 적혀 있는데, 이 부분을 숫자만 따로 뽑아서
비교하는 방식으로 다듬으면 더 좋아질 것 같다.

내 프로젝트에 가져가고 싶은 것: 외부 API 호출(ask_claude) 자체에
try/except를 걸어서, 한 번의 호출 실패가 파이프라인 전체를 죽이지 않게
만든 패턴.
```

코더가 질문에 코멘트로 답하면 리뷰 완료입니다.
