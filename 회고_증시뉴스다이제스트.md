# [회고] 에이전트 팀 꾸리기 — 증시 뉴스 다이제스트 구축 (2026-09-15)

## 프로젝트 개요

모두의연구소 "에이전트 팀 꾸리기" 과정에서 **증시 뉴스 다이제스트 에이전트**를 구축했다. 수업에서 배운 AI 뉴스레터 에이전트를 기반으로, 실제로 매일 F&G(공포·탐욕) 지수 기반 역발상 투자를 하는 내 관점에 맞춰 증시 소식을 자동으로 수집·선별·요약·검수·발행하는 파이프라인을 완성했다.

- **뉴스레터 이름**: 증시 뉴스 다이제스트
- **독자**: F&G 지수 기반 역발상 투자자 (나스닥/TQQQ 비중이 큼)
- **발행 주기**: 매일 (24시간 창)
- **발행 채널**: Discord 웹훅
- **GitHub**: https://github.com/smartman98/my-newsletter-agent

---

## 오늘 한 일

### 1. 소스 탐색 및 수집 파이프라인 구축

총 7개 소스 후보를 실제로 측정해서 5곳을 채택했다. 국내 2곳(한국경제, 연합뉴스 경제), 해외 3곳(Investing.com, Yahoo Finance, CNBC Markets)이다. RSS를 직접 호출해 HTTP 상태·24시간 내 건수·본문 추출 가능 여부를 코드로 재보고 판단했다.

### 2. 선별 로직 설계

예선(30건 묶음 → 각 묶음 5건) → 본선(상대평가, 전체 후보 비교) 2단계 구조를 구현했다. 프롬프트로 "같은 사건은 하나만, 매체 편중 없이"라고 부탁하는 것만으로는 부족해서, 본선 결과에 **이벤트 라벨 중복 제거 + 매체당 최대 2건 상한**을 코드로 한 번 더 강제했다.

### 3. 요약·검수·발행

- trafilatura로 본문 추출 → Claude(Anthropic)로 headline·summary·insight 3칸 생성
- 할루시네이션 검수: 요약/인사이트에 나온 숫자가 원문에 실제로 있는지 대조, 불합격 시 1회 재생성 후 재검사, 그래도 실패하면 스킵
- Discord 웹훅으로 실제 발행까지 성공 (2회 실행 모두 실제 발행 확인)

---

## 어려웠던 점과 해결 과정

### 소스 탈락 기준을 감으로 정했다가 정정한 것

처음엔 CNBC Markets를 "24시간 내 신규글이 1건뿐이라 죽은 소스"라는 이유로 뺐다. 그런데 이 컷오프("1건 미만이면 탈락")가 근거 없이 임의로 정한 숫자였다는 지적을 받고 나서야, G1(본문)만 통과하면 소스 단계에서 미리 판단하지 않고 선별 단계의 자연스러운 경쟁에 맡기는 쪽으로 방침을 바꿨다. 실제로 CNBC를 다시 넣고 돌려보니 수집량은 3건만 늘고(191→194건) 최종 선택엔 한 번도 안 뽑혀서, 이 방침이 결과를 해치지 않는다는 것도 실험으로 확인했다.

### AI 호출 실패로 파이프라인 전체가 죽은 것

본선(상대평가) 단계에서 Claude 호출이 "최대 턴 수 초과"로 실패하면서 전체 실행이 그대로 죽는 일이 실제로 있었다. 수집 노드는 "소스 하나가 죽어도 나머지는 모은다"는 원칙을 처음부터 지켰으면서, 정작 AI 호출 자체가 실패하는 경우는 예외처리가 없었다. `ask_claude` 함수에 try/except를 추가해서, 호출이 실패해도 빈 문자열을 돌려주고 기존 "JSON 파싱 실패 시 안전한 대체값" 경로로 자연스럽게 흡수되도록 고쳤다.

### 검수의 숫자 대조 방식이 만든 오탐

숫자를 문자열로 단순 대조하다 보니, 쉼표(`,`)가 붙은 숫자 표기(예: "5,000")의 일부가 잘려서 "500,"처럼 인식되어 실제로는 문제없는 요약이 검수에 불합격하는 오탐이 발생했다. 그래도 재생성 로직이 있어서 실행 자체는 멈추지 않았다.

---

## 배운 점

1. **탈락 기준은 "왜 이 숫자인가"를 스스로 설명할 수 있어야 한다.** 감으로 정한 컷오프는 지적받으면 바로 무너진다. 접근(G3) 관문처럼 HTTP 상태 코드로 판정되는 것과, 생존(G2)처럼 사람이 임계값을 정해야 하는 것은 근거의 확실성이 다르다는 것도 체감했다.
2. **외부 API 호출은 기본적으로 실패할 수 있다고 가정해야 한다.** 소스 수집처럼 "실패가 예상되는 곳"뿐 아니라, AI 호출처럼 "당연히 될 거라 생각한 곳"에도 예외처리가 있어야 한다는 걸 실제 크래시로 배웠다.
3. **프롬프트에 적은 부탁은 확률적으로만 지켜진다.** "같은 사건은 하나만"이라고 아무리 강조해도 코드로 한 번 더 확인하지 않으면 언젠가 어긴다.
4. **검수 로직도 완벽하지 않다.** 문자열 대조 방식은 구현이 쉽지만 표기 차이(쉼표, 단위)에 취약하다 — 그래도 "실패하면 재생성, 그래도 안 되면 스킵"이라는 안전장치가 있으면 완벽하지 않은 검수도 실전에서 쓸 수 있다.

---

## 최종 실행 결과

```
① 수집   24시간 창 · 194건 (소스 5곳, 무응답/0건: 없음)
② 선별   194건 → 묶음 7개 → 예선 35건 → 본선 5건 → 중복/상한 제거 0건 → 최종 5건
   취재 실패(본문 부족): [포토] 코스피 6700선 붕괴…3.26% 하락
④ 검수   4건 → 3건 합격 (재생성 1건 시도, 재생성 후에도 실패해 1건 스킵)
⑤ 발행   3건 · Discord 발행 성공 ✓
```

---

## 향후 개선 방향

- 검수를 "숫자만 따로 뽑아 원문과 다시 대조"하는 방식으로 바꿔서 쉼표·단위 표기 오탐 줄이기
- Yahoo Finance처럼 24시간 내 갱신이 적은 소스는 시간 창을 넓히거나 별도 취급
- GitHub Actions 또는 cron으로 매일 아침 자동 발행 구성
- AI 호출 실패율 자체를 `store/metrics.jsonl`에 기록해서 추적

---

## 기술 스택

| 구분 | 사용 기술 |
| --- | --- |
| 워크플로 | LangGraph StateGraph |
| LLM | Anthropic Claude (claude_agent_sdk) |
| 수집 | feedparser, requests |
| 본문 추출 | trafilatura |
| 구조화 출력 | JSON 프롬프팅 + 정규식 파싱(자체 구현, OpenAI response_format 미사용) |
| 발행 | Discord Webhook |
| 설정 | YAML (audience.yaml) |
